# Copy to .env and change these before running `docker compose up`.
# docker-compose.yml reads this file automatically.
DB_ROOT_PASSWORD=change_me_root_password
DB_NAME=shield_jobportal
DB_USER=shield_user
DB_PASS=change_me_password
